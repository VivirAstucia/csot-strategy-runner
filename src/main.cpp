/*
Accept exactly 2 arguments after the program name: strategy path and CSV path.
If the count is wrong, print a short usage message and exit nonzero.
For now, you do not need to load the strategy or parse the CSV yet.
You just want the executable to prove the program shape is correct.
*/

#include <iostream>
#include <string>
#include "strategy.hpp"
